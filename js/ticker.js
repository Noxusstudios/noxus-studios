/* ============================================================
   NOXUS — ticker.js
   Ticker is CSS-only (per spec §6 rule 8). This file exists
   for symmetry with the spec file structure; reserved for
   ticker enhancements (pause-on-hover, programmatic seeding).
   ============================================================ */
